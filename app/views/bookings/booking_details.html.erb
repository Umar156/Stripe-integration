<div class="col-lg-8 mt-5">
  <div class="card shadow mb-3 mt-2">
    <div class="card-body">
      <h4 class="card-title"><%= @booking.workshop.name %></h4>
      <div class="row mt-3">
        <div class="col-md-5">
          <h4><%= @booking.customer.name %>/ <%= @booking.customer.email %> </h4>
        </div>
      </div>
    </div>
  </div>
</div>